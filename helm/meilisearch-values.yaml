primary:
  resources:
    limits: { cpu: 200m, memory: 256Mi }
    requests: { cpu: 50m, memory: 128Mi }
  persistence:
    enabled: false
service: { type: ClusterIP }
# helm/meilisearch-values.yaml
tests:
  enabled: false         # official meilisearch chart
# fallback if using Bitnami:
testFramework:
  enabled: false
